{
  "name": "einarinau",
  "private": true,
  "packageManager": "pnpm@9.15.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  },
  "dependencies": {
    "@types/node": "^20.17.19"
  }
}